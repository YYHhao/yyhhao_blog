[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "yyhhao-markdown-compat"
version = "1.0.0"
dependencies = ["Markdown>=3.7"]

[tool.setuptools]
py-modules = ["blog_markdown"]
